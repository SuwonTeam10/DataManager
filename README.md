                        ## 🚀 MLOps 통합 관리 시스템 (C#)
본 프로젝트는 파이썬 기반 MLOps 파이프라인을 원격(SSH) 및 로컬 환경에서 통합 관리하기 위한 C# 기반 관리 시스템입니다.

-----------------------------------------------------------------------------------------------------------------------------------

### ⚙️ Core Features

#### 1. 아키텍처 설계 (ICE.cs)
- **통합 인터페이스:** `ICommandExecutor`를 통해 로컬(Python 프로세스)과 원격(SSH.NET) 실행 방식을 추상화하여 통일.
- **RemoteExecutor:** `SshClient`와 `ShellStream`을 이용한 실시간 로그 스트리밍 구현.
- **LocalExecutor:** `Process` 객체를 활용한 로컬 파이썬 실행 및 로그 파싱 구현.

#### 2. 로그인 및 연동 (LoginForm.cs)
- **기능:** IP, 아이디, 비밀번호 입력, 보기/숨기기(마스킹), 모두 지우기 기능.
- **사용성:** Enter 키를 통한 입력 칸 이동 및 자동 로그인 시도.
- **데이터:** 로그인 정보 로컬 파일 저장(Persistence) 및 워터마크(Placeholder)/에러 메시지 출력.

#### 3. UI 연동 (Form1.cs)
- **모드 전환:** 라디오 버튼을 통한 로컬/원격 모드 전환.
- **안전성:** 원격 모드 선택 시 로그인 폼 호출, 성공 시 `_executor` 자동 세팅 및 취소 시 로컬 모드로 자동 복구되는 UI 흐름 설계.
- **데이터 시각화:** 실시간 로그 파싱 및 차트 데이터 갱신 구조 완성.
- 
-----------------------------------------------------------------------------------------------------------------------------------

### 📋 2026-05-25 업데이트 사항 (v1.0.1)
- **UI/UX 개선:** 유저 친화적인 UI 구성 및 상단바 레이아웃 최적화.
- **버그 수정:** AI 학습 모듈 및 모델 테스트 단계에서 발생하던 오류 해결.
- **아키텍처:** 로컬/원격(SSH) 실행 인터페이스(`ICommandExecutor`) 기반의 통합 구조 구현.

### 🏗️ 아키텍처 상세
- **ICE.cs (ICommandExecutor):** 로컬(Python 프로세스)과 원격(SSH.NET) 실행 방식을 추상화하여 통일.
- **RemoteExecutor:** SshClient/ShellStream을 이용한 실시간 로그 스트리밍 구현.
- **LocalExecutor:** Process를 활용한 로컬 파이썬 실행 및 로그 파싱 구현.

### 🔑 로그인 및 연동
- **LoginForm.cs:** 자동 로그인, 정보 파일 저장(Persistence), 입력 마스킹, 워터마크 기능 구현.
- **Form1.cs:** 라디오 버튼을 통한 모드 전환 및 로컬/원격 모드 전환 흐름 설계.

### 🚧 TODO & FIXME (향후 작업 예정)
- **[FIXME] AI 학습 및 모델 테스트 오류 수정 보완:**
  - 현재 모델 테스트 단계에서 발생하는 예외 상황에 대한 에러 핸들링 로직 강화가 필요합니다.
  - 학습 루프 실행 중 데이터 파싱 오류가 발생할 수 있는 엣지 케이스 점검 필요.
- **향후 계획:**
  - 실시간 로그 파싱의 속도 최적화.
  - 원격 접속 끊김(Time-out) 방지를 위한 재접속 로직(Keep-alive) 구현.

 -----------------------------------------------------------------------------------------------------------------------------------

 ### ⚡ 2026-05-26 변경 사항 (v1.0.2)
- **최종 보완 작업:**
  - AI 학습/테스트 모듈 내 예외 처리 로직 추가로 안정성 향상.
  - 로그인 폼 입력값 유효성 검사 및 UI 피드백 로직 개선.
  - 폼 전환 시 로컬/원격 상태 동기화 정밀화.
  - (여기에 마지막에 작업하신 구체적인 기능이 있다면 한 줄 더 적어주세요!)

### 🏗️ 아키텍처 상세 (업데이트)
- **ICommandExecutor:** 인터페이스를 통한 실행 방식 통합 완료.
- **Log Parsing:** 실시간 스트리밍 데이터의 파싱 정밀도 향상.

### 🚧 TODO & FIXME (향후 작업)
- **[FIXME] AI 학습 및 모델 테스트:** 예외 상황 발생 시 사용자에게 명확한 에러 메시지 출력 기능 추가 예정.
- **향후 계획:**
  - 원격 접속 세션 관리(Keep-alive) 로직 구현.
  - 로그 뷰어 차트의 렌더링 성능 최적화.
